# kugou-unlock APK 开发指南

本文是后续 Android 原生版本的设计基线，不代表当前已经提供 APK。目标是复用并验证
现有解密算法，同时遵守 Android 沙箱、分区存储和用户授权边界。

## 1. 产品目标

首版应做到：

- 由用户通过系统文件选择器选择加密音频、输出目录和已导出的密钥文件。
- 以文件内容优先识别 KGG、KGM、KGMA、VPR 和已是明文的音频。
- 批量解密、显示逐项进度、支持取消、失败重试与可导出的诊断报告。
- 可选读取用户手动选择的 KRC 目录和歌曲数据库，生成同名 LRC/封面并写入标签。
- 所有歌曲、密钥和数据库仅在设备本地处理。

首版不做：

- 不绕过 Android 应用沙箱，不静默读取酷狗私有目录。
- 不默认依赖 root、Shizuku 或 ADB。
- 不提供在线歌曲下载、账号登录、版权规避或密钥分享。
- 不把 Python 解释器整体打包进 APK；先实现可测试的 Kotlin/JNI 核心。

## 2. 技术选型

建议使用 Kotlin、Jetpack Compose、Material 3，最低 SDK 26，目标 SDK 跟随当前稳定版。
文件访问使用 Storage Access Framework：

- ACTION_OPEN_DOCUMENT：选择 mggkey、数据库或单个音频。
- ACTION_OPEN_DOCUMENT_TREE：选择输入/输出/KRC 目录。
- takePersistableUriPermission：仅对用户明确选择的 URI 持久授权。

长任务使用前台服务，并显示通知、进度和取消按钮。WorkManager 只适合可延迟且能安全
重试的补全或校验任务；用户正在等待的批量解密应由前台服务负责。

建议模块：

~~~text
app/                 Compose UI、导航、权限与设置
domain/              任务模型、错误码、用例
crypto-core/         格式嗅探、TEA、QMC2、KGM、流式解密
android-storage/     DocumentFile、ContentResolver、原子输出
metadata/            MMKV、SQLite/KRC、封面与标签
test-vectors/        无版权的合成向量、文件头和边界样本
~~~

## 3. 核心算法迁移

优先把 tea.py、qmc2.py、kgm.py 和格式嗅探移植为纯 Kotlin。使用 ByteArray、显式
Little Endian 读写和无符号位运算，避免平台默认符号扩展。每个算法都必须与 Python
实现的固定向量逐字节对比。

若 Kotlin 性能不足，再只把热点流密码循环移入 Rust/C++ JNI：

- JNI 接口只接收数据块和不可变密钥，不接触 Android URI。
- Kotlin 层负责 I/O、取消、进度、错误处理和临时文件。
- 原生库启用 arm64-v8a；是否提供 armeabi-v7a/x86_64 由兼容矩阵决定。
- 禁止在日志、崩溃报告或异常文本中输出 ekey、完整 hash 映射或音频内容。

KGG 必须保留当前的 MAP 变体探测：先验证标准映射，再验证 legacy same-shift 映射，
只有解密头符合受支持音频容器才接受。不要仅凭文件后缀选择算法。

## 4. 文件识别与命名

统一识别顺序：

1. 读取至少 24 字节，先检查明文容器魔数。
2. 检查 KGM/VPR 魔数与 offset 20 的版本或模式。
3. 内容不足以判断时，从复合后缀最右侧向左查找 kgg、kgm、kgma、vpr。
4. 不能可靠判断则标为 UNKNOWN，不尝试猜测解密。

输出基名从最靠右的加密后缀开始剥离全部尾随后缀。例如
artist.song.kgg.flac.download 输出 artist.song，加上解密后实际嗅探到的容器扩展名。

使用同一输出目录时要检测重名。推荐“询问一次并记住”：跳过、覆盖、自动编号。
覆盖必须先写同目录临时 Document，再在成功校验后替换；无法原子重命名的 Provider
要明确显示降级行为。

## 5. 密钥与数据来源

普通 Android 应用不能读取另一个应用的 /data/user/0 私有目录，这是平台安全边界。
APK 首版只支持：

- 用户选择已经导出的 mggkey_multi_process，应用在本地解析。
- 用户选择已经生成的 kgg.key。
- KGM/KGMA/VPR 使用内置公开算法，不需要设备数据库。

将来可单独设计 root 或 Shizuku 适配器，但必须满足：

- 作为可选模块，默认关闭；首次使用前解释读取的准确路径和用途。
- 每次授权由用户明确触发，先只读检查，再复制到应用私有临时目录。
- 不修改、删除或锁定酷狗文件，不长期保存 root 权限。
- 支持正式版与概念版包名，路径探测结果必须呈现给用户确认。

## 6. 歌词、封面与标签

推荐流程与 Python 版一致：

1. 用户选择 KRC 缓存目录和 SQLite 数据库。
2. 从数据库建立 musicname、歌曲 hash、歌手、标题、专辑、封面 URL 的只读索引。
3. 使用多个已知 hash 匹配 KRC，解码并去掉逐字时间标记，生成逐行 LRC。
4. 封面 URL 只从本地数据库取得；用户开启网络补全后才下载，并使用 URL 哈希缓存。
5. 在音频旁生成同名 LRC 和 JPEG/PNG/WebP。
6. 用户启用“嵌入标签”后，写入 FLAC Picture/Vorbis Comment、Ogg
   METADATA_BLOCK_PICTURE 或 MP3 ID3 APIC/USLT。

首版可以先交付外置文件，再加入可靠的标签库。选择依赖时需确认：

- 支持 ContentResolver 流，而不是只支持真实文件路径。
- 能保留未知标签与音频帧，不重编码音频。
- 许可证与 GPL-3.0 分发兼容。
- 对损坏文件、超大封面和播放器占用有明确错误。

数据库 schema 会随酷狗版本变化。为每个已验证版本提供独立 Adapter，并以列存在性
探测选择；禁止为了“兼容”执行数据库写入或迁移。

## 7. UI 与任务状态

主界面建议分为四步：选择来源、识别预览、处理选项、进度与结果。预览应显示算法、
识别依据、预期输出名以及是否找到密钥。

每个文件使用稳定状态机：

~~~text
QUEUED -> SNIFFING -> WAITING_FOR_KEY -> DECRYPTING -> VERIFYING
       -> ENRICHING -> SUCCEEDED
任意处理中状态 -> FAILED 或 CANCELLED
~~~

取消时停止读取下一块，关闭流并删除本次未完成的临时输出。成功文件绝不因其它文件
失败而回滚。进程重建后可从 Room 中恢复任务清单，但重新打开 URI 前先检查持久权限。

## 8. 安全与隐私

- 密钥映射、数据库和歌词默认保存在应用私有目录；任务结束可由用户一键清理。
- 日志只记录文件显示名、阶段、错误码和脱敏 hash 前缀。
- 封面下载使用 HTTPS、限制响应大小和 MIME，并拒绝重定向到非 HTTPS。
- 不加载数据库扩展，不执行来自数据库的 SQL，只运行固定只读查询。
- 对 KRC 解压设置输入与输出上限，防止压缩炸弹。
- 对所有长度、偏移和整数运算做边界检查。
- 发布版关闭调试日志，不集成包含密钥或文件内容的遥测。

## 9. 测试策略

单元测试至少覆盖：

- TEA、QMC2 Map/RC4、legacy same-shift、KGM v3 的黄金向量。
- 缺失后缀、多重复合后缀、大小写混合、误导后缀和截断文件头。
- 0 字节、仅头部、超短数据块、非 4 KiB 对齐和大文件流式处理。
- KRC 正常/损坏/超限、数据库缺表/缺列/WAL 不完整。
- 输出重名、空间不足、权限撤回、Provider 不支持重命名、任务取消。

黄金向量应由合成明文生成，或只保留极短的无版权测试数据；不得提交商业歌曲、
真实 mggkey、用户数据库或可关联个人账号的 hash 集合。

设备测试至少包含 Android 8、11、13 和当前目标版本，以及本地存储、DocumentsUI、
常见第三方文件 Provider。比较 Kotlin/APK 与 Python 对相同向量的 SHA-256 输出。

## 10. 里程碑

### M0：规格冻结

- 整理 Python 行为、错误码、测试向量和格式识别优先级。
- 对当前 QMC2 两种 MAP 变体建立回归测试。

### M1：只读识别器

- Compose 文件选择、SAF 权限、批量扫描。
- 显示真实格式、加密类型、输出名，不产生文件。

### M2：KGM 家族解密

- 流式 KGM/KGMA/VPR v3、原子输出、校验、取消。
- 在多 Android 版本上完成大文件和异常路径测试。

### M3：KGG 与密钥

- MMKV/kgg.key 导入、hash 匹配、QMC2 两变体。
- 密钥缺失提示和本地脱敏诊断报告。

### M4：歌词与封面

- 用户手动选择数据库/KRC、生成外置文件。
- 网络封面开关、缓存管理；标签嵌入作为后续子里程碑。

### M5：发布准备

- 无障碍、中文/英文、隐私说明、GPL 源码与第三方许可证清单。
- 签名、可复现构建、SBOM、release APK/AAB 和升级测试。

## 11. 构建与发布约束

- 使用 Gradle Version Catalog 和依赖锁定，版本固定后再更新。
- CI 执行 lint、单元测试、仪器测试、依赖许可证与已知漏洞检查。
- release 构建启用 R8，但要为标签库/JNI 保留必要规则。
- 仓库现有许可证是 GPL-3.0；APK 的源代码、修改和分发方式必须满足该许可证。
- 发布前复核酷狗客户端版本兼容矩阵，并在界面中声明“本地文件工具、非酷狗官方产品”。

## 12. 开发启动清单

真正开始 APK 开发时，先完成这些决策：

- 确定最低/目标 SDK、ABI 和是否首版包含 JNI。
- 固化 Python 1.2 行为与黄金向量。
- 选定兼容 GPL-3.0 且支持流式 URI 的标签库。
- 确认首版只支持手动导入，不接入 root/Shizuku。
- 制作不含真实歌曲与个人数据的端到端测试包。

完成清单后再创建 Android 工程，避免先搭 UI、后发现核心 I/O 或许可证路线不可行。
